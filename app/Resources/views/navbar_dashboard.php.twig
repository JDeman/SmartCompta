<nav class="navbar navbar-default navbar-custom navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header page-scroll" style="margin-top: 12px; margin-left: 10px">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a href="{{ path('dashboard_user') }}">
                    <img src="{{asset('img/new2_logo_nav.png')}}"/>
                </a>
            {% else %}
                <a href="{{ path('page_d_accueil') }}">
                    <img src="{{asset('img/new2_logo_nav.png')}}"/>
                </a>
            {% endif %}

            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <div class="collapse navbar-collapse"style="background-color: white;" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('dashboard_user') }}" style="color: deepskyblue">Accueil</a></li>
                <li><a href="{{ path('fos_user_profile_show') }}" style="color: deepskyblue">Mon compte</a></li>
                {#<li><a href="{{ path('fos_user_profile_edit') }}">Modifier mon profil</a></li>#}
                <li><a href="{{ path('show_entreprise_user') }}" style="color: deepskyblue">Mon entreprise</a></li>
                <li><a href="{{ path('mremi_contact_form') }}" style="color: deepskyblue">Aide</a></li>
                <li>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {% block name %}
                        {% endblock name %}
                            <a href="{{ path('fos_user_security_logout') }}" style="color: deepskyblue">
                                {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                            </a>
                    {% else %}
                        <a href="{{ path('fos_user_security_login') }}" style="color: deepskyblue">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>